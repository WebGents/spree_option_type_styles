source 'https://rubygems.org'

gem 'spree', github: 'spree/spree', branch: '3-1-stable'
# Provides basic authentication functionality for testing parts of your engine
gem 'spree_auth_devise', github: 'spree/spree_auth_devise', branch: '3-1-stable'
# ActiveRecord::Enum feature to work fine with I18n and simple_form.
gem 'enum_help'
# This gem embeddes the jQuery colorpicker in the Rails asset pipeline.
gem 'jquery-minicolors-rails'

gemspec
